[project]
name = "personal-diary"
version = "0.1.0"
description = ""
authors = [
    {name = "Dima"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django (>=5.2,<5.3)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
